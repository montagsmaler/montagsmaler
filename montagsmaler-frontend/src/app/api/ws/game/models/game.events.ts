export enum GameEvents {
  JOIN_GAME = 'join_game',
  PUBLISH_IMAGE = 'publish_image',
  GET_GAME = 'get_game',
  GAME_STARTED = 'game_started',
  GAME_OVER = 'game_over',
  ROUND_STARTED = 'round_started',
  ROUND_OVER = 'round_over',
  IMAGES_SHOULD_PUBLISH = 'images_should_publish',
  IMAGE_ADDED = 'image_added'
}
